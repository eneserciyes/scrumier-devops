kubectl delete all -l app=config app=discovery-server
kubectl delete all -l app=config app=discovery-server
kubectl delete all -l app=config app=discovery-server
kubectl delete all -l app=config app=discovery-server
